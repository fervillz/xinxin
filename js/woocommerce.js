/**
 * Theme Customizer enhancements for a better user experiences.
 *
 * Contains handlers to make Theme Customizer preview reload changes asynchronously.
 */

( function( $ ) {

	$( "td" ).remove( ".product-quantity" );

} )( jQuery );
